{
	"name": "meteor-mongo-files",
	"version": "0.1.1",
	"description": "A Meteor library to make file uploading and managing as easy as possible. It offers a consistent and resilient file storage strategy, relying on busboy, mongodb and gridFS. The files are stored in MongoDB. The package is very lightweight (~25kB minified) and has only busboy as direct dependency.",
	"main": "src/main.js",
	"files": [
		"dist/**/*"
	],
	"scripts": {
		"build": "webpack",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"mongo",
		"files",
		"mongodb",
		"storage",
		"meteor",
		"gridfs",
		"upload",
		"stream",
		"express",
		"express.js"
	],
	"author": "Jonathan Zappalà",
	"repository": {
		"type": "git",
		"url": "https://github.com/jonisapp/meteor-mongo-files"
	},
	"license": "MIT",
	"dependencies": {
		"busboy": "^1.6.0"
	},
	"devDependencies": {
		"webpack": "^5.73.0",
		"webpack-bundle-analyzer": "^4.5.0",
		"webpack-cli": "^4.10.0"
	}
}
